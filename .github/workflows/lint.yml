name: Lint
on:
  push:
    branches:
      - main
      - 3.2.5_patch
  pull_request:
    paths-ignore:
      - "**/*.md"
jobs:
  lint:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run lints
        run: make lint
