---
"posthog-ios": patch
---

Use Swift 6 access-controlled imports for vendored implementation modules and enable library evolution for framework builds, removing implementation-only import warnings without exposing private module dependencies to SDK consumers.
