---
"posthog-ios": patch
---

Add an experimental `sessionReplayConfig.screenshotModeBackgroundCapture` option for Session Replay screenshot mode, allowing screenshot rendering to be scheduled on a background queue to reduce main-thread pressure.
